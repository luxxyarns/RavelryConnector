import XCTest
@testable import RavelryConnector

final class RavelryConnectorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RavelryConnector().text, "Hello, World!")
    }
}
